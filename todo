Utilisation d un template :
    Cherecher iu utiliser Bootstrap-admin comme template de ce projet 

Memebre du projet : 
    RAKOTOARIVONY Harena Natolotra Sarobidy  ETU-3940    Alias : [H]
    FENOHERYLIANTSOA Ny Aina Andreane        ETU-4199    Alias : [L]
    FANEVA Jedidia                           ETU-4042    Alias : [J]

Fonctionnalite 1 :
    1 - Back offcie (admin) => [H]
        a - Login admin (mettre le login par defaut sur le formulaire)
            back:
            -route:
            .route get admin/login
            .route post admin/login (essaie de connection)
            -controllers:
            Authcontrollers:*verification username et mot de passe
                            *stocker les donnes dans une session
            -repository:
            UserRepository:*findbyusername()
            front:
            .page login pour admin et user

        b - Gestion des categories
    2 - Front  office (utilisateur) => [L]
        a - . Créer une page d’inscription et de login [H]
        b. Page pour gérer ses objets 
            i. Objet : titre description, 1 ou plusieurs photos, prix estimatif 
        c. Page pour voir la liste des objets des autres utilisateurs 
            i. Fiche objet 
                1. Proposition d’échange avec notre objet 
        d. Page pour gérer les échanges avec d’autres utilisateurs 
            i. Liste des propositions 
            ii. Acceptation ou refus 
Fonctionnalite 2 :
    Dans modele : 
    ajoute un verification des sessions s il y en a 
    sinon on le remet dans login admin
    
    1 - Back office (admin) => [J]
        a. Statistiques 
            [OK]i. Nombre d’utilisateur inscrit 
                [OK]Back : 
                    route : GET / admin_users [OK]
                    controller : AdminController->showUsers() [OK]
                    repositories : UserRepository->findAll() [OK]

                [OK]Front :
                    badge nb_utilisateur inscrit utilisateur inscrit[OK]
                    badge nb_admin [OK]
                    badge nb_user[OK]
                    [OK]ajouter des photo de profil :
                        mettre les pdp dans public/assets/images/pdp[OK]
            [OK]ii. Le nombre d’échange effectué
                [OK]Back :
                    route : GET / admin_echange [OK]
                    controller : AdminController->showEchanges() [OK]
                    repositories : EchangeRepository->findAll() [OK]
                [OK]Front : 
                    tableau table [OK]
    
    2 - Front office (utilisateur) => [J]
        [OK]a. Barre de recherche
            [OK]- Vue SQL : `v_objets_cat` (objets join catégories) 
                [OK]— fichier `database/20260211-03-v_objets_cat.sql`.
            [OK]- Backend : `ObjetRepository::search($q,$category)` 
                - route `GET /objets/search` 
                [OK]-controller: `ObjetController::search()` 
                [OK]- ciew `objet/search.php`.
            [OK]- Frontend : formulaire `q` + `category`, 
                [OK]-afficher résultats en cartes (image, titre, catégorie, extrait, lien détail).

        b. Historique d'appartenance
            - Backend : `EchangeRepository::getOwnershipHistory($objetId)` 
                - route `GET /objet/{id}/history` 
                - `ObjetController::history()` 
                - vue `objet/history.php`.
            - Frontend : page `objet/history.php` 
                -affichant timeline/list des propriétaires (avatar/initiale, nom, date, statut).

    Notes : adapter les chemins d'images selon stockage (uploads/ ou assets/images). Pagination recommandée si nécessaire.

    Bonus : fragmente les card des objet pour ameliorer les recherches dans _card.php